<%= form_with(model: school, local: true) do |form| %>
	<div class="field">
		<%= form.label :name %>
		<%= form.text_field :name %>
	</div>

	<div class="field">
		<%= form.label :princile%>
		<%= form.text_field :princile %>
	</div>

	<div class="actions">
		<%= form.submit %>
	</div>
<% end %>
